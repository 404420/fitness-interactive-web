# Next 10 Coding Tasks

1. Add normalized `health` state to `eliteFitnessData` with daily summaries and sync status.
2. Add browser-safe HealthKit bridge functions: request permissions, receive native data, and fallback to mock data.
3. Create a normalized daily health summary from sleep, sleep stages, resting heart rate, HRV, steps, and workouts.
4. Update recovery score input so it can be built from normalized health data.
5. Add a dashboard health/recovery card showing sleep score, recovery score, recommended intensity, and data source.
6. Add deterministic daily workout recommendation logic using recovery, previous workouts, activity level, and user goal.
7. Show daily recommendation reasoning text in the UI.
8. Store daily recommendation snapshots by date.
9. Create `HEALTHKIT_SETUP.md` documenting iOS bridge architecture and limitations.
10. Add automated tests for the recovery score and recommendation outputs.

